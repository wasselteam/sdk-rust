id = "rust-fibonacci"
name = "Rust Fibonacci Plugin"
version = "0.0.0"
component = "${CARGO_TARGET_DIR:../../target}/wasm32-wasip2/release/rust_fibonacci.wasm"
endpoint = "/rust/fibonacci/"

[build]
cmd = "cargo build --target wasm32-wasip2 --release"
